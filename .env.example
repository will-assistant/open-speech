# ─────────────────────────────────────────────────────────────────────────────
# Open Speech — Configuration
# Copy to .env and edit as needed. All values shown are defaults.
# ─────────────────────────────────────────────────────────────────────────────

# ── Server ───────────────────────────────────────────────────────────────────
# STT_HOST=0.0.0.0
# STT_PORT=8100

# ── Speech-to-Text (STT) ────────────────────────────────────────────────────
# STT_DEVICE=cuda                                          # cuda or cpu
# STT_COMPUTE_TYPE=float16                                 # float16, int8, int8_float16
# STT_DEFAULT_MODEL=deepdml/faster-whisper-large-v3-turbo-ct2
# STT_PRELOAD_MODELS=                                      # Comma-separated models to load on startup

# ── STT Model Lifecycle ─────────────────────────────────────────────────────
# STT_MODEL_TTL=300                                        # Seconds idle before auto-unload (0=never)
# STT_MAX_LOADED_MODELS=0                                  # Max models in memory (0=unlimited)

# ── STT Streaming (WebSocket) ───────────────────────────────────────────────
# STT_STREAM_CHUNK_MS=2000                                 # Audio chunk size in ms
# STT_STREAM_VAD_THRESHOLD=0.5                             # Voice activity detection threshold
# STT_STREAM_ENDPOINTING_MS=300                            # Silence before finalizing utterance
# STT_STREAM_MAX_CONNECTIONS=10                            # Max concurrent WebSocket streams

# ── Text-to-Speech (TTS) ────────────────────────────────────────────────────
# TTS_ENABLED=true                                         # Enable/disable TTS endpoints
# TTS_DEFAULT_MODEL=kokoro                                 # Default TTS engine
# TTS_DEFAULT_VOICE=af_heart                               # Default voice
# TTS_DEVICE=                                              # cuda/cpu (inherits STT_DEVICE if empty)
# TTS_MAX_INPUT_LENGTH=4096                                # Max input text characters
# TTS_DEFAULT_FORMAT=mp3                                   # Default output format
# TTS_DEFAULT_SPEED=1.0                                    # Default speech speed
# TTS_PRELOAD_MODELS=                                      # Comma-separated TTS models to preload
# TTS_VOICES_CONFIG=                                       # Path to custom voice presets YAML

# ── Security ─────────────────────────────────────────────────────────────────
# STT_API_KEY=                                             # API key (empty=auth disabled)
# STT_RATE_LIMIT=0                                         # Requests/min per IP (0=disabled)
# STT_RATE_LIMIT_BURST=0                                   # Burst allowance
# STT_MAX_UPLOAD_MB=100                                    # Max upload file size in MB
# STT_CORS_ORIGINS=*                                       # Comma-separated CORS origins
# STT_TRUST_PROXY=false                                    # Trust X-Forwarded-For headers
